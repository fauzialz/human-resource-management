# Override block applied to every backend service that connects to Postgres/Redis.
# .env uses localhost values for local development; docker-compose overrides them
# here so containers reach each other by service name instead.
x-backend-env: &backend-env
  POSTGRES_HOST: postgres
  REDIS_HOST: redis
  EMPLOYEE_SERVICE_URL: http://employee-service:3001
  ATTENDANCE_SERVICE_URL: http://attendance-service:3002

services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_EMPLOYEE_DB: ${POSTGRES_EMPLOYEE_DB:-employee_db}
      POSTGRES_ATTENDANCE_DB: ${POSTGRES_ATTENDANCE_DB:-attendance_db}
      POSTGRES_AUDIT_DB: ${POSTGRES_AUDIT_DB:-audit_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER}']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  gateway:
    build:
      context: .
      dockerfile: docker/Dockerfile.nestjs
      args:
        APP_NAME: gateway
    restart: unless-stopped
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      <<: *backend-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  employee-service:
    build:
      context: .
      dockerfile: docker/Dockerfile.nestjs
      args:
        APP_NAME: employee-service
    restart: unless-stopped
    ports:
      - '3001:3001'
    env_file:
      - .env
    environment:
      <<: *backend-env
    volumes:
      - employee_uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  attendance-service:
    build:
      context: .
      dockerfile: docker/Dockerfile.nestjs
      args:
        APP_NAME: attendance-service
    restart: unless-stopped
    ports:
      - '3002:3002'
    env_file:
      - .env
    environment:
      <<: *backend-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  log-consumer:
    build:
      context: .
      dockerfile: docker/Dockerfile.nestjs
      args:
        APP_NAME: log-consumer
    restart: unless-stopped
    env_file:
      - .env
    environment:
      <<: *backend-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  employee-app:
    build:
      context: .
      dockerfile: docker/Dockerfile.react
      args:
        APP_NAME: employee-app
        VITE_BASE_API_URL: ${VITE_BASE_API_URL}
    restart: unless-stopped
    ports:
      - '4000:80'

  admin-app:
    build:
      context: .
      dockerfile: docker/Dockerfile.react
      args:
        APP_NAME: admin-app
        VITE_BASE_API_URL: ${VITE_BASE_API_URL}
    restart: unless-stopped
    ports:
      - '4001:80'

volumes:
  postgres_data:
  employee_uploads:
